<%= link_to 'Create New Entry', new_entry_path, id: 'create_link' %>

<%= render partial: 'entry_group', locals: { group_name: "Kihon", entries: @kihon_entries } %>
<%= render partial: 'entry_group', locals: { group_name: "Kata", entries: @kata_entries } %>
<%= render partial: 'entry_group', locals: { group_name: "Kumite", entries: @kumite_entries } %>
<%= render partial: 'entry_group', locals: { group_name: "Drills", entries: @drill_entries } %>
<%= render partial: 'entry_group', locals: { group_name: "Senior Visits", entries: @senior_visit_entries } %>
<%= render partial: 'entry_group', locals: { group_name: "Special Training", entries: @special_training_entries } %>
<%= render partial: 'entry_group', locals: { group_name: "Experiences", entries: @experience_entries } %>

<% if @other_entries.any? %>
  <%= render partial: 'entry_group', locals: { group_name: "Other", entries: @other_entries } %>
<% end %>

