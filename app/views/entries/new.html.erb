<h1>Editing entry</h1>

<%= form_tag entries_path do %>
  <%= render partial: 'form_entry' %>
  <%= submit_tag 'Save' %>
<% end %>

<br />

<% if @entry.id.present? %>
  <%= link_to 'Show', show_entry_path(@entry) %> |
<% end %>
<%= link_to 'Back', entries_path %>
